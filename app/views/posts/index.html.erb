<% content_for(:title, "My Blog Post") %>

<h1>My Blog Posts</h1>

<ul class="lead list-unstyled">
  <% @posts.each do |post| %>
    <li><%= post.created_at.strftime("%B %d, %Y") %> - <%= post.title %></li>
  <% end %>
</ul>
